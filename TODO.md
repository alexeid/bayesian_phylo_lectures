# BIOSCI 700 Lecture Slide TODO

Course material review for Semester 1, 2026. Lectures ordered by priority.

---

## BIOSCI 322 (Phylogenetics and Coalescence, Aug 2026)

### L12: Birth-death models, macroevolution and the species tree (`lecture-macroevolution`)
- [x] First draft written from scratch for stage-3 in a new folder `lecture-macroevolution` (existing advanced `lecture-birth-death` preserved); L12 in `biosci322.html` repointed to it.
- [ ] Review the draft's pitch and pacing (13 content slides): diversity-imbalance hook -> two directions in time -> Yule (interactive) -> birth-death -> reconstructed tree -> LTT/pull-of-the-present (interactive) -> identifiability limit (Louca & Pennell) -> rate shifts -> fossilised birth-death -> nested generative model tying back to L11.
- [ ] **Preview the two interactive Plotly figures in a browser** (`yule-plot`, `ltt-plot`): served, not file://, since they `fetch('macro_data.json')`. Confirm slider/buttons render and the pull-of-the-present is legible. Data generated by `/tmp/bdsim.py` (250 surviving replicates/eps, seed 42); regenerate via that script if parameters change.
- [ ] CC-BY figures used (Harmon PCM Figs 10.1, 10.2, 11.3, 12.4) in `figures/` — credited on-slide; confirm they read well at stage-3 or swap.
- [ ] Optional: add a named empirical example (a clade's radiation, or an NZ example) alongside the Fig 12.4 phylorate tree.
- [ ] `lectures.yml` entry uses `section: biosci322`, so it does not appear on the home index grid; add a card/section if you want it discoverable there.

---

## Critical

### L5: Parsimony (`lecture-parsimony`)
- [ ] Extract text from `CharactersSupportTree.png` — add HTML description of convergence/parallelism examples
- [ ] Extract text from `Reversals.png` — add HTML caption explaining the reversal diagram
- [ ] Extract text from `Parsimony.png` — currently image-only with bullet points below; crop or replace
- [ ] Extract text from `branch-and-bound.png` — no caption or description at all
- [ ] Extract text from `InternalStorage.png` — table data should be an HTML table
- [ ] Extract text from `LengthOfCharacter.png` — tree diagram with character states; add description
- [ ] Extract text from `fitchAlgorithm.png` — entire Fitch algorithm shown only as image; add HTML walkthrough
- [ ] Overall: 7 of 15 slides are image-heavy; this is the least accessible lecture in the course

### L4: Phylogenetics (`lecture-phylogenetics`)
- [ ] Fix malformed HTML table at lines 129-138: `<tr>` used instead of `</tr>` for row closing tags
- [ ] Audit image-only slides and extract key text (15+ slides are image-heavy)

---

## High Priority

### L7: Bayesian Inference (`lecture-bayesian-inference`)
- [ ] Fix broken href on Jaynes citation (line 35): `href=""` should link to the book or a DOI

### L2: Homology and Alignment (`lecture-homology`)
- [ ] Fix dead link (lines 241, 263): `http://profs.scienze.univr.it/~liptak/ALBioinfo/...` (Eddy 2004 paper) — find working URL or DOI
- [ ] Fix dead link (lines 805, 897, 925): `http://librarysearch.auckland.ac.nz/primo_library/...` (Durbin et al textbook) — replace with stable reference
- [ ] Replace 2 deprecated `<font color="">` tags (line 186-187) with `<span style="color:...">`

### L8: Bayesian Phylogenetics (`lecture-bayesian-phylogenetics`)
- [ ] Add `<h2>` heading or caption to the BEAST software image slide (line ~285) — currently image-only
- [ ] Update BEAST software slide — citation counts and version numbers are out of date

---

## Medium Priority

### L1: Darwin's Computer (`lecture-darwins-computer`)
- [ ] Slides 2-3: Tree of Life and Descent with Modification still have text baked into images (URLs, captions) — partially addressed but could be further improved
- [ ] Verify `-8%` margin-bottom crop on tree slides (4-6) hides image text cleanly across screen sizes

### L3: MSA (`lecture-msa`)
- [ ] Add captions to progressive misalignment image sequence slides
- [ ] Verify all DOI links still resolve

### L9: Relaxed Phylogenetics (`lecture-relaxed-phylogenetics`)
- [ ] Add alt-text or HTML descriptions for tree space geometry diagrams (conceptually complex SVGs)

### L10: The Coalescent (`lecture-coalescent`)
- [ ] Add static fallback content for Wright-Fisher JavaScript simulation (for non-JS contexts like PDF export)

---

## Low Priority / Polish

### All lectures
- [ ] Add `alt` attributes to `<img>` tags across all lectures for screen reader accessibility
- [ ] Audit for consistent heading hierarchy (some lectures use mostly `<h2>`, others mix `<h2>`/`<h3>`)

### L2: Homology and Alignment
- [ ] Add explanatory context to isolated globin comparison image slides (lemur, goldfish, soybean)

### L6: Substitution Models (`lecture-substitution-models`)
- [ ] No issues found — consider as template for quality standard

### L11-L13: Comparative Methods, Brownian Motion, Beyond Brownian
- [ ] No issues found — these are the cleanest lectures in the course

---

## Completed

- [x] L1: Trim from 37 to 20 slides for 50-minute slot
- [x] L1: Convert slides 16 (Phylodynamics) and 21 (Trees inside trees) to HTML
- [x] L1: Extract baked-in text from tree enumeration slides (4-6), Tree of Life (2), Descent with modification (3), Phylogenies and fossil record (25)
- [x] L1: Move 18 commented-out slides to appropriate advanced lectures
- [x] Create `lecture_sa` directory with fossil dating slides
